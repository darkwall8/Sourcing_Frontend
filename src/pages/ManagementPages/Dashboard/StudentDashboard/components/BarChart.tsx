import { useEffect, useRef } from "react";
import { Chart, BarController, ArcElement, Tooltip, Legend } from "chart.js";

Chart.register(BarController, ArcElement, Tooltip, Legend);

const BarChart = () => {
    useEffect(()=>{

    })

    return (
        <>

        </>
    )
} 

export default BarChart